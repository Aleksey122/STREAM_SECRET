name: Rust Android Build
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install Rust
        run: rustup target add aarch64-linux-android
      - name: Build
        # Запускаем сборку из корня, указывая путь к Cargo.toml
        run: cargo build --manifest-path rust_core/Cargo.toml --target aarch64-linux-android --release
      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: librust_core
          path: rust_core/target/aarch64-linux-android/release/librust_core.so
