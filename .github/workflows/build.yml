name: Build
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    container:
      image: kivy/buildozer:latest
    steps:
      - uses: actions/checkout@v4
      - name: Build
        run: |
          # В этом контейнере уже есть всё. Просто запускаем сборку.
          buildozer android debug
