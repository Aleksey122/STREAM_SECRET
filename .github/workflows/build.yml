name: Build
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build
        run: |
          sudo apt-get update
          sudo apt-get install -y python3-pip
          pip3 install --user --upgrade buildozer
          # Принудительное принятие через yes
          yes | buildozer android debug
